-- Task Manager's default shortcut for Omarchy Quattro.
-- Add this line to ~/.config/hypr/bindings.lua after checking Super+K for conflicts.
-- Opens or focuses Task Manager; never closes an application directly.
o.bind("SUPER + ALT + DELETE", "Task Manager", "omarchy-task-manager")
